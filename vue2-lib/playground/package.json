{
  "name": "vue2-lib-playground",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:preview": "vite build --mode preview",
    "build:intranet": "vite build --mode intranet",
    "routes": "node scripts/generateRoutes.mjs"
  },
  "devDependencies": {
    "@vitejs/plugin-vue2": "^1.1.2",
    "vue-router": "3.5.3"
  }
}
